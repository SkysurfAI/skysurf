"""Internal implementation modules.

Everything here is private API. Names and shapes may change in any
release without warning. Public callers should only import from
:mod:`skysurf` (the top-level package re-exports the stable surface).
"""
